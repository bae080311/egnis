# 이그니스 카페24 골라담기 옵션 UI 과제

## 과제 개요
카페24 상품 상세페이지(`/product/detail.html`)에서 **기본 구매 흐름을 유지**하면서 **골라담기 형태의 옵션 UI**를 구현하는 코딩 과제. 평가 포인트는 디자인 완성도가 아니라 **카페24 구조 이해 + 기능 안정성**이다.

피그마 시안: https://www.figma.com/design/Mix9lnBDClaeNNhDpTuho2/-공유용-골라담기-UI

## 절대 규칙 (반드시 지킬 것)
1. **테스트몰에서만 작업.** 운영몰 절대 금지.
2. **카페24 기본 구매/장바구니 로직 재구현 금지.** 그대로 활용한다. 기본 옵션 영역(`module="product_option"` 등)은 숨겨도 되지만, 필요한 DOM·이벤트(`{$form.option}`, `{$form.quantity}`, `{$action_buy}`, `{$action_basket}`, `module="product_action"` 등)는 삭제하지 않는다.
3. **외부 설정 JS는 옵션 UI 구성값(뱃지·설명·추천문구·추가가능횟수·가격표시) 관리 전용.** 구매 로직을 대체하는 용도로 쓰지 않는다.
4. **작업 범위는 PC/모바일 "옵션 영역"과 "옵션 동작"에 한정.** 상품 이미지, 상세설명, 배너, 구매버튼 외형 등 옵션과 무관한 영역은 수정하지 않는다.
5. **계정 정보·API 키 등 민감정보는 어떤 산출물에도 포함 금지.**
6. **옵션값 suffix(`_1`, `_2`, ...) 규칙**: 같은 개입수의 옵션값이 n개 존재하면, 그 개입수는 최대 n번까지만 추가할 수 있어야 한다.
   - 예: `30개입_1`·`30개입_2`가 있으면 최대 2번, `50개입_1`만 있으면 최대 1번까지 추가 가능.
7. **모바일**: 모달 방식 또는 옵션영역 인라인 노출 방식 중 선택. 단, 터치 영역과 구매 버튼 영역이 겹치면 안 된다.
8. 반응형은 PC 브라우저 개발자도구의 모바일 뷰로 확인 가능. 화면 폭이 좁아져도 레이아웃이 깨지면 안 된다.

## 테스트 상품 옵션 설정
설정 경로: 상품 → 상품등록 → 옵션/재고 설정

| 항목 | 설정값 |
|---|---|
| 옵션 사용 | 사용함 |
| 옵션 구성방식 | 독립 선택형 |
| 옵션 설정 | 직접 입력하기 |
| 옵션 스타일 | 텍스트버튼 |
| 필수옵션 | 체크 |
| 옵션값 | 10개입_1, 10개입_2, 30개입_1, 30개입_2, 50개입_1, 50개입_2, 100개입_1, 100개입_2 |
| 금액 설정 | 개입 수마다 금액 다르게 (구성별 차이 있도록) |

## 구현 요구사항 체크리스트 (9개)

| # | 요구사항 |
|---|---|
| 1 | 골라담기 형태 커스텀 옵션 UI 정상 노출 |
| 2 | 커스텀 옵션 선택 시 카페24 기본 텍스트버튼 옵션의 선택 상태·구매 흐름과 함께 갱신 |
| 3 | 옵션 선택 후 선택상품 목록 정상 생성, 반복 클릭·재선택 시 오류/중복 없음 |
| 4 | 옵션값 suffix(_1,_2) 기준 개입수별 추가 가능 횟수 제한 |
| 5 | 개입 수별 금액/추가금액 정확히 계산, 옵션값 수만큼 상품 품목 모두 생성 |
| 6 | 수량 변경, 총금액 계산, 장바구니 담기, 바로구매 정상 동작 |
| 7 | 선택된 옵션의 활성화 상태가 사용자에게 명확히 표시 |
| 8 | 옵션별 뱃지·설명·추천문구·추가가능횟수·가격표시 등이 외부 설정 JS로 분리 |
| 9 | PC/모바일 반응형에서 옵션 UI 레이아웃 안 깨짐 |

우대 사항(시간 여유 시): 설정 JS 구조를 문서화해 다른 작업자도 값만 바꿔 쓸 수 있게 구성 / 피그마 시안 수준으로 카드 간격·색상·선택표시·모바일 배치 다듬기.

## 제출물
1. **적용 URL**: 실제 방문자가 보는 스토어프론트 URL(`https://내몰도메인.cafe24.com/product/detail.html?product_no=...`). 관리자 편집창 URL(`/disp/admin/editor/...`) 아님. 시크릿 창(로그아웃 상태)에서 커스텀 옵션 UI가 실제로 보이는지 확인 필수 — 저장만 하고 "대표 디자인" 지정을 안 하면 반영되지 않는다.
2. **Git 또는 수정 파일**: 로컬 버전관리 했다면 Git 링크, 관리자 편집창에서 직접 작업했다면 HTML/CSS/JS zip.
3. **README**: ① 옵션 UI 구현 방식 ② 외부 설정 JS 구조 ③ 카페24 옵션과 동기화한 방식.
4. **체크리스트 기준 QA 결과**: 특히 #2(동기화), #3(중복 방지), #4(횟수 제한), #7(활성화 상태 표시)은 GIF/영상 첨부 권장.
5. 미구현 항목·제한 사항·개선 아이디어.

제출 방식: 테스트 안내 이메일로 회신 / 기한: 안내 후 영업일 기준 2일 내 / 예상 소요: 약 8시간.

## 핵심 파일
- `detail.html` — 카페24 스마트디자인 베이직 스킨의 `/product/detail.html` (현재 888줄, 순정 상태).
  - 옵션 영역: 163~563줄 부근 (`module="product_option"`, `module="product_quantity"`, 옵션 추가 시 상품이 append되는 영역 등, `{$form.option}` / `{$form.quantity}` / `{$form.add_option}` 치환 태그 사용)
  - 구매 버튼 영역: 573~603줄 (`module="product_action"`, `{$action_buy}` 바로구매, `{$action_basket}` 장바구니)
  - `<script>`/`<style>` 태그는 없음 — JS/CSS는 `<!--@js(경로)-->` / `<!--@css(경로)-->` 형태로 카페24 서버의 스킨 파일을 참조 (로컬엔 실제 파일 없음). 새 옵션 UI용 JS/CSS는 이 컨벤션에 맞춰 별도 파일로 추가하거나, 카페24 편집창에서 허용하는 방식으로 인라인 삽입한다.
- 로컬에서 편집한 내용은 카페24 관리자 스마트디자인 편집창에 붙여넣어야 실제 스토어프론트에 반영된다.
